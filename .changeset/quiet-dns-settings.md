---
"openwrtctl": minor
---

Configure AdGuard Home upstream DNS servers, bootstrap DNS servers, and upstream mode through `config.yaml`.

Existing configurations must add `adguard.upstreamDns`, `adguard.bootstrapDns`, and `adguard.upstreamMode` before upgrading.
